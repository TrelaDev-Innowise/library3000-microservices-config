spring:
  config:
    activate:
      on-profile: k8s

consul:
  discovery:
    instance-id: auth-service-${spring.application.instance-id:${random.value}}
    healthCheckUrl: "http://${HOSTNAME}:8081/actuator/health"
    prefer-ip-address: false
    hostname: auth-service